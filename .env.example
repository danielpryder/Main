# Port for the web UI / API (default 3000)
PORT=3000
# Cache lifetime for provider responses, in seconds (default 120)
CACHE_TTL_SECONDS=120
# Per-provider request timeout, in milliseconds (default 15000)
PROVIDER_TIMEOUT_MS=15000
